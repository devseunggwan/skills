---
name: <skill-name>
description: >
  <One-line description of what this skill does — no trigger phrases here.>
when_to_use: >
  Triggers on "<keyword1>", "<keyword2>", "<Korean-keyword>".
# Optional runtime fields — each is documented at
# https://code.claude.com/docs/en/skills.md; add only when the condition holds:
#
# disable-model-invocation: true
#   The skill runs only when the user types /praxis:<skill-name>; the model
#   never auto-loads it and Skill(...) cannot reach it (RUNTIME_CONSTRAINTS.md
#   §2). Never on a skill another skill chains into via Skill(...).
#
# allowed-tools:
#   - Read
#   - Bash(gh pr view *)
#   Tools the steps run without a permission prompt for that turn. List only
#   what the steps actually run, as specific Bash(cmd *) prefixes rather than
#   bare Bash, and never a mutation command (gh pr merge, git push).
#
# Uncomment after live round-trip verification (required when wrapping an
# external CLI, calling AskUserQuestion, or delegating via Skill(...)):
# verified-against-runtime: true
# runtime-verified-at: YYYY-MM-DD
# runtime-verified-note: "<cli-name> <version> — one-line observed behavior"
---

# <Skill Title>

## Overview

<2–3 sentences: what problem this skill solves and why it exists.>

**Core principle:** <one-sentence invariant the skill enforces.>

## When to Use

- <condition or signal that warrants using this skill>
- <condition 2>

## Process

### Step 1: <Step Name>

<What to do in this step. Include concrete commands or tool calls.>

```bash
# example command
```

### Step 2: <Step Name>

<Next action. Keep steps atomic — one logical operation per step.>

### Step N: Report

<How to surface results to the user: format, required fields, one-line summary.>

## Error Handling

| Error | Recovery |
|-------|----------|
| <error condition> | <what to do> |
| Prerequisite CLI not found | Abort with install instructions |

## Limitations

- <known limitation 1>
- <known limitation 2>
